# Required for the /contact page to actually deliver email.
# Get a key at https://resend.com — without it, the contact form will show a clear
# error to visitors instead of silently failing.
RESEND_API_KEY=
